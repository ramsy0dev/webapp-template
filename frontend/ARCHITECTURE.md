/**
 * Frontend Architecture Overview
 */

# 🏗️ Frontend Architecture Overview

## Application Structure

```
┌─────────────────────────────────────────────────────────────────────┐
│                        FRONTEND APPLICATION                         │
├─────────────────────────────────────────────────────────────────────┤
│                                                                       │
│  ┌──────────────────────────────────────────────────────────────┐   │
│  │                        ROOT LAYOUT                           │   │
│  │  ┌────────────────────────────────────────────────────────┐  │   │
│  │  │  HEADER (Navigation + Auth Links)                     │  │   │
│  │  └────────────────────────────────────────────────────────┘  │   │
│  │  ┌────────────────────────────────────────────────────────┐  │   │
│  │  │  PAGE CONTENT (Routes)                                │  │   │
│  │  │                                                         │  │   │
│  │  │  ┌──────────┐  ┌──────────┐  ┌──────────┐            │  │   │
│  │  │  │   HOME   │  │  LOGIN   │  │REGISTER  │            │  │   │
│  │  │  └──────────┘  └──────────┘  └──────────┘            │  │   │
│  │  │  ┌──────────────┐                                     │  │   │
│  │  │  │  DASHBOARD   │ (Protected)                         │  │   │
│  │  │  └──────────────┘                                     │  │   │
│  │  └────────────────────────────────────────────────────────┘  │   │
│  │  ┌────────────────────────────────────────────────────────┐  │   │
│  │  │  FOOTER (Links + Info)                                │  │   │
│  │  └────────────────────────────────────────────────────────┘  │   │
│  └──────────────────────────────────────────────────────────────┘   │
│                                                                       │
└─────────────────────────────────────────────────────────────────────┘
```

## Component Hierarchy

```
App (root.tsx)
├── Layout
│   ├── Header
│   │   ├── Logo
│   │   └── Navigation Links
│   ├── Routes
│   │   ├── Home Page
│   │   ├── Login Page
│   │   │   ├── Input (email)
│   │   │   ├── Input (password)
│   │   │   ├── Button
│   │   │   └── Alert
│   │   ├── Register Page
│   │   │   ├── Input (name)
│   │   │   ├── Input (email)
│   │   │   ├── Input (password)
│   │   │   ├── Input (confirm password)
│   │   │   ├── Button
│   │   │   └── Alert
│   │   └── Dashboard (Protected)
│   │       ├── Card (stat 1)
│   │       ├── Card (stat 2)
│   │       ├── Card (stat 3)
│   │       └── Card (content)
│   └── Footer
│       ├── Links
│       └── Copyright
└── Error Boundary
```

## Data Flow Architecture

```
┌─────────────────────────────────────────────────────────────┐
│                    USER INTERACTION                         │
│                    (Form Submit, Click)                     │
└────────────────────┬────────────────────────────────────────┘
                     │
                     ▼
┌─────────────────────────────────────────────────────────────┐
│                   EVENT HANDLER                             │
│        (handleSubmit, handleChange, onClick)                │
└────────────────────┬────────────────────────────────────────┘
                     │
                     ▼
┌─────────────────────────────────────────────────────────────┐
│                   COMPONENT STATE                           │
│          (useState, useForm, useApi)                        │
└────────────────────┬────────────────────────────────────────┘
                     │
                     ▼
┌─────────────────────────────────────────────────────────────┐
│                  API CLIENT (lib/api.ts)                    │
│        (GET, POST, PUT, PATCH, DELETE)                      │
└────────────────────┬────────────────────────────────────────┘
                     │
                     ▼
┌─────────────────────────────────────────────────────────────┐
│                  BACKEND API                                │
│          (http://localhost:8000)                            │
└────────────────────┬────────────────────────────────────────┘
                     │
                     ▼
┌─────────────────────────────────────────────────────────────┐
│                   RESPONSE                                  │
│           (Data, Error, Status)                             │
└────────────────────┬────────────────────────────────────────┘
                     │
                     ▼
┌─────────────────────────────────────────────────────────────┐
│              STATE UPDATE                                   │
│        (setData, setError, setLoading)                      │
└────────────────────┬────────────────────────────────────────┘
                     │
                     ▼
┌─────────────────────────────────────────────────────────────┐
│              RE-RENDER COMPONENT                            │
│          (Show data, error, or loading state)               │
└─────────────────────────────────────────────────────────────┘
```

## Folder Structure with Descriptions

```
frontend/
│
├── app/
│   │
│   ├── routes/                    # Page components (full pages)
│   │   ├── home.tsx              # Landing page
│   │   ├── login.tsx             # Login form
│   │   ├── register.tsx          # Registration form
│   │   ├── dashboard.tsx         # Protected dashboard
│   │   └── +types/               # Auto-generated types
│   │
│   ├── components/                # Reusable UI components
│   │   ├── common.tsx            # Button, Input, Card, Alert, Loader
│   │   └── layout.tsx            # Header, Footer
│   │
│   ├── lib/                       # Utility functions & hooks
│   │   ├── api.ts                # API client class
│   │   ├── auth.ts               # Auth utilities
│   │   ├── hooks.ts              # Custom React hooks
│   │   ├── utils.ts              # Helper functions
│   │   └── constants.ts          # Constants & config
│   │
│   ├── services/                  # API service classes
│   │   ├── auth.service.ts       # Auth API calls
│   │   └── user.service.ts       # User API calls (example)
│   │
│   ├── root.tsx                   # Root layout component
│   ├── routes.ts                  # Route definitions
│   ├── app.css                    # Global styles
│   └── welcome/                   # Welcome component
│
├── Configuration Files
│   ├── .env.example              # Environment variables template
│   ├── tailwind.config.ts        # Tailwind CSS config
│   ├── vite.config.ts            # Vite bundler config
│   ├── tsconfig.json             # TypeScript config
│   └── package.json              # Dependencies
│
├── Documentation
│   ├── README.md                 # Main documentation
│   ├── BOILERPLATE_SUMMARY.md   # Quick overview
│   ├── FRONTEND.md              # Detailed guide
│   ├── DEVELOPMENT_GUIDE.md     # Best practices
│   ├── FILES_INDEX.md           # File reference
│   └── SETUP_COMPLETE.sh        # Setup completion message
│
└── Public Assets
    └── public/                   # Static files
```

## Technology Stack

```
┌────────────────────────────────────────────────────┐
│                 FRONTEND STACK                    │
├────────────────────────────────────────────────────┤
│                                                    │
│  Framework & Routing                              │
│  • React 19.x                                     │
│  • React Router 7.x                               │
│  • TypeScript 5.x                                 │
│                                                    │
│  Styling                                          │
│  • Tailwind CSS 4.x                               │
│  • CSS Utilities                                  │
│                                                    │
│  Build & Dev Tools                                │
│  • Vite 7.x                                       │
│  • ESBuild                                        │
│                                                    │
│  Utilities                                        │
│  • Native Fetch API                               │
│  • localStorage for token storage                 │
│  • React Hooks                                    │
│                                                    │
└────────────────────────────────────────────────────┘
```

## Authentication Flow

```
┌─────────────────────────────────────────────────────────────┐
│                   AUTHENTICATION FLOW                       │
├─────────────────────────────────────────────────────────────┤
│                                                              │
│  1. USER VISITS LOGIN PAGE                                  │
│     └─> localhost:5173/login                                │
│                                                              │
│  2. ENTER CREDENTIALS & SUBMIT FORM                         │
│     └─> Email + Password sent to API                        │
│                                                              │
│  3. BACKEND VALIDATES & RETURNS TOKEN                       │
│     └─> POST /auth/login → { access_token: "..." }          │
│                                                              │
│  4. TOKEN STORED IN LOCALSTORAGE                            │
│     └─> localStorage.setItem("auth_token", token)           │
│                                                              │
│  5. USER REDIRECTED TO DASHBOARD                            │
│     └─> navigate("/dashboard")                              │
│                                                              │
│  6. PROTECTED ROUTES CHECK TOKEN                            │
│     └─> useAuthRequired() validates auth status             │
│                                                              │
│  7. API REQUESTS INCLUDE TOKEN                              │
│     └─> Sent in request headers / body                      │
│                                                              │
│  8. ON LOGOUT                                               │
│     └─> Clear token & redirect to login                     │
│                                                              │
└─────────────────────────────────────────────────────────────┘
```

## Component Communication

```
Parent Component
│
├─> Props Down ────────────────────> Child Component
│                                     (Receives data)
│
└─> Events Up <────────────────────── Child Component
                                     (Callbacks)

Example: Parent (Dashboard)
│
├─> Pass user data ──────────────────> UserCard
│
└─> Handle delete <────────────────── UserCard
                                     (onClick callback)
```

## State Management Pattern

```
Component State (hooks)
│
├─> useState          - Local component state
│   └─> values, loading, error
│
├─> useForm           - Form state management
│   └─> values, errors, touched, isSubmitting
│
├─> useAuth           - Authentication state
│   └─> isAuthenticated, isChecking
│
├─> useApi            - API request state
│   └─> data, loading, error, execute
│
└─> localStorage      - Persistent state
    └─> auth_token
```

## API Integration Pattern

```
Service Layer (services/*.service.ts)
│
├─> AuthService.login()
├─> AuthService.register()
├─> UserService.getUsers()
└─> UserService.updateUser()
        │
        ▼
API Client (lib/api.ts)
│
├─> GET /endpoint
├─> POST /endpoint (with data)
├─> PUT /endpoint (with data)
├─> PATCH /endpoint (with data)
└─> DELETE /endpoint
        │
        ▼
Hooks (lib/hooks.ts)
│
├─> useApi(method, endpoint)
│   └─> { data, loading, error, execute }
│
└─> useForm(initialValues, onSubmit)
    └─> { values, errors, touched, isSubmitting, ... }
```

## Page Protection Flow

```
User visits /dashboard
│
▼
useAuthRequired() hook runs
│
├─> Check if authenticated
│   └─> Get token from localStorage
│
├─> If not authenticated
│   └─> Redirect to /login
│
├─> If checking
│   └─> Show Loader
│
└─> If authenticated
    └─> Render protected content
```

## Error Handling Flow

```
API Request
│
├─> Success Response
│   └─> Parse JSON → setData
│
├─> Error Response
│   ├─> Non-200 status
│   ├─> Parse error details
│   └─> setError + throw
│
└─> Network Error
    └─> Catch block → formatError()
        └─> Show user-friendly message
```

## Deployment Architecture

```
┌──────────────────────────────────────────────┐
│          PRODUCTION DEPLOYMENT               │
├──────────────────────────────────────────────┤
│                                              │
│  Source Code (TypeScript + React)            │
│           ↓                                  │
│  Build Process (npm run build)               │
│           ↓                                  │
│  Optimized Output                            │
│  ├── build/client/  (Static assets)          │
│  └── build/server/  (Node server)            │
│           ↓                                  │
│  Deploy to:                                  │
│  ├── Vercel                                  │
│  ├── Netlify                                 │
│  ├── Docker Container                        │
│  └── Custom Server                           │
│           ↓                                  │
│  Frontend App running at:                    │
│  └── https://yourapp.com                     │
│                                              │
└──────────────────────────────────────────────┘
```

---

This architecture provides:
- ✅ Clear separation of concerns
- ✅ Reusable components and utilities
- ✅ Scalable API integration
- ✅ Type safety with TypeScript
- ✅ Professional styling with Tailwind
- ✅ Production-ready patterns
